# Distill - Environment Variables
# Copy this file to .env and fill in the values you need.
# All variables are optional — distill works without any of them.
#
# Quick start:  ./scripts/setup.sh

# ─── Postiz (social media publishing) ──────────────────────────────
# Run: cd docker/postiz && docker compose up -d
# Then log in at https://localhost:6106 to find your API key under user settings.
# The setup script (scripts/setup.sh) configures these automatically.
POSTIZ_URL=https://localhost:6106/api/public/v1
POSTIZ_API_KEY=
# POSTIZ_DEFAULT_TYPE=draft
# POSTIZ_SLACK_CHANNEL=general
# POSTIZ_SCHEDULE_ENABLED=true
# POSTIZ_TIMEZONE=America/New_York
# POSTIZ_WEEKLY_TIME=09:00
# POSTIZ_WEEKLY_DAY=0
# POSTIZ_THEMATIC_TIME=09:00
# POSTIZ_THEMATIC_DAYS=1,2,3
# POSTIZ_INTAKE_TIME=17:00

# ─── Social Platform Keys (used by Postiz) ──────────────────────
# Create apps at each platform's developer portal, then restart Postiz:
#   cd docker/postiz && docker compose up -d
#
# Slack: https://api.slack.com/apps → Create App → Basic Information
#   Redirect URL: https://localhost:6106/api/integrations/social-connect/slack
# SLACK_ID=
# SLACK_SECRET=
# SLACK_SIGNING_SECRET=
#
# Twitter/X: https://developer.twitter.com/en/portal
# X_API_KEY=
# X_API_SECRET=
#
# LinkedIn: https://www.linkedin.com/developers/apps
# LINKEDIN_CLIENT_ID=
# LINKEDIN_CLIENT_SECRET=
#
# Reddit: https://www.reddit.com/prefs/apps
# REDDIT_CLIENT_ID=
# REDDIT_CLIENT_SECRET=

# ─── Ghost CMS (newsletter publishing) ───────────────────────────
# GHOST_URL=https://your-ghost-instance.com
# GHOST_ADMIN_API_KEY=your-key-id:your-secret
# GHOST_NEWSLETTER_SLUG=your-newsletter

# ─── Reddit (saved/upvoted content ingestion) ────────────────────
# Create an app at https://www.reddit.com/prefs/apps
# REDDIT_CLIENT_ID=your-client-id
# REDDIT_CLIENT_SECRET=your-client-secret
# REDDIT_USERNAME=your-username
# REDDIT_PASSWORD=your-password

# ─── YouTube (watch history + transcripts) ────────────────────────
# Get an API key at https://console.cloud.google.com
# YOUTUBE_API_KEY=your-api-key

# ─── Gmail (newsletter ingestion) ─────────────────────────────────
# Requires OAuth2 credentials from Google Cloud Console.
# Download credentials.json and pass via --gmail-credentials flag.
